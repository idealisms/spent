# Dependency upgrade status

Tracks the major-version dependency upgrades for `tracker/`. Started after
adding the Puppeteer e2e test suite (#145) gave a safety net (build + 89
unit tests + a real-browser render) to upgrade against.

Each group below landed as its own PR, verified with: `tsc --noEmit`,
`eslint app`, `yarn test`, `yarn build`, `yarn test:e2e`.

## Done

| Group                  | Packages                                                                                                                                                                                                                                                                            | PR                                                  |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| 1. Trivial / isolated  | `memoize-one` 6, `redux-thunk` 3, `react-google-charts` 5, `@types/jest` 30, `@testing-library/jest-dom` 7, `@types/redux-mock-store` 1.5. Also removed dead deps: `express`, `react-hot-loader`, `react-transform-catch-errors`, `react-transform-hmr`, `redbox-react`, `.babelrc` | [#146](https://github.com/idealisms/spent/pull/146) |
| 2. Lint/format tooling | `prettier` 3, `globals` 17, `eslint-webpack-plugin` 6                                                                                                                                                                                                                               | [#147](https://github.com/idealisms/spent/pull/147) |
| 3. Jest                | `jest` 30 (`ts-jest` bumped to latest 29.x, which already supports jest 30)                                                                                                                                                                                                         | [#148](https://github.com/idealisms/spent/pull/148) |
| 4. React               | `react`/`react-dom`/`@types/react`/`@types/react-dom` 19. Removed dead `react-test-renderer` (deprecated upstream in favor of `@testing-library/react`) and a stale `resolutions` pin                                                                                               | [#149](https://github.com/idealisms/spent/pull/149) |
| 5. MUI                 | `@mui/material`/`@mui/icons-material`/`@mui/system` 9, `@mui/x-date-pickers` 9, `@date-io/moment` 3                                                                                                                                                                                 | [#150](https://github.com/idealisms/spent/pull/150) |
| 6. react-router-dom    | `react-router-dom` 7 (no code changes — this app only uses the classic declarative API, which v7's compat package still exports as-is)                                                                                                                                              | [#151](https://github.com/idealisms/spent/pull/151) |
| 7. Misc types          | `@types/google.visualization` 0.0.75                                                                                                                                                                                                                                                | this change                                         |

## Blocked — revisit periodically

| Package(s)                                                               | Wants                                          | Blocked on                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Check                                                                                             |
| ------------------------------------------------------------------------ | ---------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `eslint`, `@eslint/js`                                                   | 9.39.5 → 10.x                                  | `eslint-plugin-react` latest (7.37.5) caps its peer dep at `eslint ^9.7` — no v10-compatible release yet                                                                                                                                                                                                                                                                                                                                                                                  | `npm view eslint-plugin-react peerDependencies`                                                   |
| `typescript`                                                             | 5.9.3 → 7.x                                    | `ts-jest` (`typescript: '>=4.3 <7'`) and `@typescript-eslint/{eslint-plugin,parser}` (`typescript: '>=4.8.4 <6.1.0'`) both explicitly exclude TS7. Note TS "7" is not a normal major — the TS team never shipped a stable 6.x (only betas/nightlies) and renamed the release to 7.0 as part of switching to their new Go-based compiler, so this is a bigger jump than the version number suggests. First stable (7.0.2) shipped 2026-07-08 — still under 2 months old as of this writing | `npm view ts-jest peerDependencies`, `npm view @typescript-eslint/eslint-plugin peerDependencies` |
| `webpack`, `webpack-cli`, `webpack-dev-middleware`, `webpack-dev-server` | 5.105.0 → 5.110.3+ (and majors for the others) | `webpack` past 5.105.0 breaks the production build with a module-interop error in `@mui/system`/`@mui/x-date-pickers` ESM imports (`export 'default' ... was not found`) — bisected during #146, confirmed still broken on 5.110.3. Root cause is upstream (webpack or one of the loaders), not this app's code                                                                                                                                                                           | `yarn up webpack@latest && yarn build` in a scratch branch                                        |
| `puppeteer`                                                              | 21.11.0 → 22+                                  | Deliberately pinned. 22+ dropped the CommonJS build (ESM-only), which `ts-jest`'s CommonJS output can't `require()`. Revisit only alongside a wider ESM migration of the e2e test setup (see `tracker/e2e/README.md`)                                                                                                                                                                                                                                                                     | n/a — a deliberate pin, not upstream-blocked                                                      |
